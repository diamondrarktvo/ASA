import { MainScreen, Row, Text } from "_shared";

const VersionCheck = () => {
  return (
    <Row>
      <Text variant={"tertiary"}>Version 0.0.1</Text>
    </Row>
  );
};

export default VersionCheck;
